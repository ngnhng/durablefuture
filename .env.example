# DurableFuture Configuration

# Docker build configuration
DOCKER_PLATFORM=linux/amd64
TARGETARCH=amd64
MODE=debug

# NATS Configuration
# NATS server connection URL (default: nats://localhost:4222)
NATS_URL=nats://localhost:4222

# NATS server host (default: localhost)
NATS_HOST=localhost

# NATS server port (default: 4222)
NATS_PORT=4222

# NATS client configuration
# Maximum number of reconnection attempts (-1 for unlimited, default: -1)
NATS_MAX_RECONNECTS=-1

# Time to wait between reconnection attempts (default: 2s)
NATS_RECONNECT_WAIT=2s

# Drain timeout for graceful shutdown (default: 30s)
NATS_DRAIN_TIMEOUT=30s

# Ping interval for connection health checks (default: 2m)
NATS_PING_INTERVAL=2m

# Maximum number of outstanding pings (default: 2)
NATS_MAX_PINGS_OUT=2

# NATS client name for identification (default: durablefuture-client)
NATS_CLIENT_NAME=durablefuture-client

# Server Configuration
# Server host for CLI defaults (default: localhost)
SERVER_HOST=localhost

# Server port for CLI defaults (default: 4222)
SERVER_PORT=4222

# Timeout Configuration
# Request timeout for client operations (default: 10s)
REQUEST_TIMEOUT=10s

# Activity Simulation Configuration
# Time to simulate credit card processing (default: 10s)
CHARGE_PROCESSING_TIME=10s

# Time to simulate package shipping (default: 3s)
SHIP_PROCESSING_TIME=3s

# Time for delayed activity simulation (default: 1s)
DELAYED_ACTIVITY_TIME=1s

# Number of days for shipping estimates (default: 3)
SHIPPING_DAYS=3